# npm install playground

Fresh npm project for testing `glimpse-cli` as an npm-installed dependency rather than from this repository's source tree.

## Use

```sh
npm install
npm test
```

The test suite executes the installed `node_modules/.bin/glimpse` binary and verifies basic CLI behavior.
